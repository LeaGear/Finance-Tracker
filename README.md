# Finance Tracker (Python CLI) 💰

Простой и эффективный консольный трекер финансов, написанный на Python. Позволяет вести учет доходов и расходов, просматривать статистику по категориям и фильтровать записи по месяцам.

## ✨ Основные функции

* **Учет операций:** Добавление доходов и расходов с указанием категории и комментария.
* **Уникальные ID:** Каждая операция получает уникальный идентификатор (UUID4).
* **Статистика:** Просмотр суммарных трат или доходов, сгруппированных по категориям.
* **Баланс:** Мгновенный расчет текущего финансового состояния.
* **Фильтрация:** Возможность просмотреть историю операций за конкретный месяц.
* **Автосохранение:** Данные сохраняются в формате JSON и автоматически загружаются при старте.

## 🛠 Технологический стек

* **Язык:** Python 3.x
* **Формат данных:** JSON (для хранения базы операций)
* **Библиотеки:** `uuid` (для ID), `datetime` (для работы со временем), `json`.

## 📂 Структура проекта

* `main.py` — точка входа, управление основным меню.
* `operations.py` — логика добавления, просмотра истории и баланса.
* `obj_constructor.py` — конструктор объектов транзакций.
* `stats.py` — расчет аналитики по категориям.
* `filters.py` — фильтрация данных по датам.
* `storage.py` — работа с файловой системой (загрузка и сохранение).
* `optimization.py` — вспомогательные функции для валидации ввода.

## 🚀 Как запустить

1. Убедитесь, что у вас установлен Python 3.10 или выше.
2. Скачайте все файлы проекта в одну папку.
3. Запустите основной файл:
   ```bash
   python main.py
4. Или 
    ```bash
    git clone https://github.com/LeaGear/finance-tracker.git
    cd finance-tracker
    python main.py